/** Pixels from character centre to projectile spawn point. */
export const FIRE_OFFSET = 40;

/**
 * Default projectile launch speed (pixels per frame at full power).
 * Used by AimingSystem's trajectory preview when the active weapon does not
 * declare its own `trajectorySpeed`.
 */
export const DEFAULT_FIRE_SPEED = 15;
